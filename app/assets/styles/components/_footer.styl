@require '../_variables'
@require '../_mixins'

.page-footer
	padding $layoutSpace
	text-align: center
	.tag-list
		display flex
		justify-content center
		align-items center
		flex-wrap wrap
		> li
			padding $layoutSpaceHalf
